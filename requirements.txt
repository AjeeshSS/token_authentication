Django
django rest framework
redis  cache

# pip install django
# pip install djangorestframework
# pip install django-redis

# pip install django djangorestframework django-redis (install 3 at a time)